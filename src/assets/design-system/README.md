# Design system production assets

The Atlas design package under `Design System/` did not ship logo files or a
local icon sprite set. Icons are specified as Lucide (CDN substitution in the
generated kits). This folder is reserved for any future copied production
assets (logos, custom icons) extracted from `Design System/system/assets` or
`uploads`.

Do not import files from `Design System/` at runtime.
